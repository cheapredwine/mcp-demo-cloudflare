name = "mcp-demo-server"
main = "src/index.ts"
compatibility_date = "2025-03-21"
compatibility_flags = ["nodejs_compat"]

# Private - no public URL, only accessible via Service Binding
workers_dev = false

# Enable observability for logging
[observability]
enabled = true
