{
  "name": "@mcp-demo/ai-orchestrator",
  "version": "1.0.0",
  "description": "AI Orchestrator using Worker AI with MCP tool calling",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0",
    "jose": "^6.2.2",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250321.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0",
    "wrangler": "^4.0.0"
  }
}
